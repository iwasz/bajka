<?xml version="1.0" encoding="UTF-8"?>

<!-- 
* -Guzik z napisem.
** -Czcionki TTF.
* -Przełączanie widoków (link podłaczony do tego guzika).
* Obsługa F5 i Esc.
* Obsługa eventów z klawiatury w ogóle.
* -Bitmapa.

Dodatkowe:
~~~~~~~~~~
* Layouty
** -Poprawne AABB.
** -Poprawne wyświetlanie AABB.
** -RelativeGroup
** -LinearGroup - H
** -LinearGroup - W
** -TableGroup
 -->

<beans>
  <import resource="standard.xml" />

  <Config id="config" viewportWidth="800" viewportHeight="600" autoProjection="true" windowCaption="Bajka browser, F5 to refresh">
    <ref set-as="modelManager" bean="modelManager" />
  </Config>
  
  <XmlModelManager id="modelManager" file="menu.xml" name="scene">
    <ref set-as="mainContainer" bean="#container" />
  </XmlModelManager>
</beans>

